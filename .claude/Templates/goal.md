---
title: Learning Goal — <Topic>
type: goal
topic: <topic>
created: <YYYY-MM-DD>
deadline: <YYYY-MM-DD or "open-ended">
hours_per_week: <N>
status: active
tutor_prompt_mode: unset
testing_mode: unset   # set by /tutor on first run: test | honor
coverage_pct: 0
---

# Learning Goal: <Topic>

> The goal for the **<Topic>** course (`course/<slug>/`). `/curriculum <slug>` builds
> the modules + schedule from this scope; `/tutor <slug>` teaches and tests against it,
> writing scores to `progress.md` and syncing the Knowledge Audit below.

## Target

<!-- One sentence: what you want to understand or be able to do by the deadline. -->

## Scope

**In scope:** <the sub-topics this goal covers — what you want to learn>
**Out of scope:** <explicitly excluded — downstream/adjacent topics, separate arcs>

## Subgoals

- [ ] <thematic subgoal 1>
- [ ] <thematic subgoal 2>

## Knowledge Audit

> What the knowledge base already covers vs. what must be pulled in. `Status` is a
> **readiness ladder** — how ready each item is in your base — kept in sync by `/tutor`:
> - `missing` — not in your base yet (must pull a source)
> - `pulled` — a raw source exists, but no synthesised page yet
> - `untested` — a page exists, but `/tutor` hasn't tested you on it
> - `revisit` — a page exists, tested, but scored `partial`/`wrong`
> - `covered` — a page exists and you tested `confident` (top rung)

| Item | Type | Status | Score | Last tested | Source |
|------|------|--------|-------|-------------|--------|
| <item> | paper/concept/web | missing → pulled → untested → revisit → covered | confident/partial/wrong/read/untested/— | <YYYY-MM-DD or —> | <link or — > |

## Notes

<!-- Free-form: context, open questions, prerequisites assumed, anything the audit doesn't capture. -->
